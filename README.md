# hubot-s3-copy
Hubot copies files from one S3 bucket to another

This is for moving preproduction resources to a production resource so that only resources that passed QA testing can be deployed to production.

# Commands
```
# Copies the resource (if it exists) from the origin bucket/folder to the destination bucket/folder.
QA passed on [resource]
qa passed on [resource]
```
